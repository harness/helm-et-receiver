apiVersion: v1
kind: Service
metadata:
  name: {{ include "et-receiver.fullname" . }}
  labels:
    {{- include "et-receiver.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "et-receiver.selectorLabels" . | nindent 4 }}
